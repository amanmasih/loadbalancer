#It behaives as a parent context file for a particular microservice and has higher precedence and henceforth loaded before application.properties.
#but it is mandatory to have a application.properties, if not available , bootstrap properties will not invoke
#same name must be used inside userproductservice
#Application.properties is specific to springboot-applcation
#Bootstrap.properties is specific for Spring-cloud-config and is loaded before Application.properties


spring.application.name=userauthservice-producer